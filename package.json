{
  "private": true,
  "scripts": {
    "[-- Build --]": "",
    "docs:build": "turbo run build --filter=@kitchen-sink/docs",
    "web:build": "turbo run build --filter=@kitchen-sink/web",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.1.0"
}
